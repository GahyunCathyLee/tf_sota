# BAT | highD | baseline | seed 777
base: _base.yaml
exp_tag: highD0-5
dataset: highD
feature_mode: baseline
data:
  root: data
runtime:
  seed: 777
training:
  multiagent: true
  use_importance: false
