# BAT | highD | baseline | seed 42
base: _base.yaml
exp_tag: highD0-1
dataset: highD
feature_mode: baseline
data:
  root: data
runtime:
  seed: 42
training:
  multiagent: true
  use_importance: false
