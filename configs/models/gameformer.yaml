model: gameformer
display_name: "GameFormer"
upstream_dir: "external/gameformer"
adapter_dir: "adapters/gameformer"
input:
  coordinate_frame: "waymo_like_scene"
  uses_map: true
  ego_channels: 6
  neighbor_baseline_channels: 6
  raw_neighbor_channels: 10
  neighbor_dimI_channels: 8
training:
  batch_size: 16
  epochs: 100
  lr: 0.0001
  accelerator: "gpu"
notes:
  - "Prioritize interaction_prediction, not planning, for this study."
  - "Potentially expensive on local server."
