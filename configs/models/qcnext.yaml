model: qcnext
display_name: "QCNeXt"
upstream_dir: "external/qcnet"
adapter_dir: "adapters/qcnext"
input:
  coordinate_frame: "query_centric_joint"
  uses_map: true
  ego_channels: 6
  neighbor_baseline_channels: 6
  raw_neighbor_channels: 10
  neighbor_dimI_channels: 8
training:
  batch_size: 32
  epochs: 100
  lr: 0.0005
  accelerator: "gpu"
notes:
  - "No separate official repository found; track as a QCNet-derived implementation."
  - "Implement after QCNet adapter is validated."
