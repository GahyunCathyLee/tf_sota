# Trajectron++ | highD | baseline | seed 42
#   python train.py --config configs/trajectronpp/highD0-1.yaml
base: _base.yaml

exp_tag: highD0-1
dataset: highD
feature_mode: baseline

runtime:
  seed: 42
