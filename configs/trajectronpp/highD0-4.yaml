# Trajectron++ | highD | baseline | seed 0
#   python train.py --config configs/trajectronpp/highD0-4.yaml
base: _base.yaml

exp_tag: highD0-4
dataset: highD
feature_mode: baseline

runtime:
  seed: 0
