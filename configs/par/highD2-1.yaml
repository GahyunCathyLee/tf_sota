# PAR | highD | baseline+I | seed 42
#   python adapters/par/train.py --config configs/par/highD2-1.yaml --mode full --multiagent
base: _base.yaml

exp_tag: highD2-1
dataset: highD
feature_mode: I
use_importance: true

runtime:
  seed: 42

