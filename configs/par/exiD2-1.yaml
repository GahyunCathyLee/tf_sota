# PAR | exiD | baseline+I | seed 42
#   python adapters/par/train.py --config configs/par/exiD2-1.yaml --mode full --multiagent
base: _base.yaml

exp_tag: exiD2-1
dataset: exiD
feature_mode: I
use_importance: true

runtime:
  seed: 42

