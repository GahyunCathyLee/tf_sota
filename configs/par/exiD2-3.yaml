# PAR | exiD | baseline+I | seed 3407
#   python adapters/par/train.py --config configs/par/exiD2-3.yaml --mode full --multiagent
base: _base.yaml

exp_tag: exiD2-3
dataset: exiD
feature_mode: I
use_importance: true

runtime:
  seed: 3407

