# MTP-GO | exiD | baseline | seed 0
#   python adapters/mtp_go/train.py --config configs/mtp_go/exiD0-4.yaml --mode full
base: _base.yaml

exp_tag: exiD0-4
dataset: exiD
feature_mode: baseline

runtime:
  seed: 0
