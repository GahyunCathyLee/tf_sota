# MTP-GO | highD | baseline | seed 42
#   python adapters/mtp_go/train.py --config configs/mtp_go/highD0-1.yaml --mode full
base: _base.yaml

exp_tag: highD0-1
dataset: highD
feature_mode: baseline

runtime:
  seed: 42
