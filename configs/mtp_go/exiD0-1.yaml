# MTP-GO | exiD | baseline | seed 42
#   python adapters/mtp_go/train.py --config configs/mtp_go/exiD0-1.yaml --mode full
base: _base.yaml

exp_tag: exiD0-1
dataset: exiD
feature_mode: baseline

runtime:
  seed: 42
