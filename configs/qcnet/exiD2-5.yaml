# QCNet | exiD | edge I | seed 777
base: _base.yaml

exp_tag: exiD2-5
dataset: exiD
feature_mode: baseline

runtime:
  seed: 777

training:
  multiagent: true
  use_interaction_importance: true
  normalize_i: false
