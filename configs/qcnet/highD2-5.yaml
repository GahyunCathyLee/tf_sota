# QCNet | highD | edge I | seed 777
base: _base.yaml

exp_tag: highD2-5
dataset: highD
feature_mode: baseline

runtime:
  seed: 777

training:
  multiagent: true
  use_interaction_importance: true
  normalize_i: false
