# QCNet | highD | edge I | seed 42
base: _base.yaml

exp_tag: highD2-1
dataset: highD
feature_mode: baseline

runtime:
  seed: 42

training:
  multiagent: true
  use_interaction_importance: true
  normalize_i: false
