# QCNet | exiD | edge I | seed 1234
base: _base.yaml

exp_tag: exiD2-2
dataset: exiD
feature_mode: baseline

runtime:
  seed: 1234

training:
  multiagent: true
  use_interaction_importance: true
  normalize_i: false
