# QCNet | highD | edge I | seed 0
base: _base.yaml

exp_tag: highD2-4
dataset: highD
feature_mode: baseline

runtime:
  seed: 0

training:
  multiagent: true
  use_interaction_importance: true
  normalize_i: false
