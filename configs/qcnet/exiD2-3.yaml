# QCNet | exiD | edge I | seed 3407
base: _base.yaml

exp_tag: exiD2-3
dataset: exiD
feature_mode: baseline

runtime:
  seed: 3407

training:
  multiagent: true
  use_interaction_importance: true
  normalize_i: false
