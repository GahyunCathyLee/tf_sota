# QCNet | exiD | edge I | seed 42
base: _base.yaml

exp_tag: exiD2-1
dataset: exiD
feature_mode: baseline

runtime:
  seed: 42

training:
  multiagent: true
  use_interaction_importance: true
  normalize_i: false
