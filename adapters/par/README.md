# PAR Adapter

Goal: add highD/exiD as a PAR trajectory prediction task.

First implementation tasks:

1. Clone upstream into `external/par`.
2. Copy the NuScenes car trajectory task pattern.
3. Decide whether `dim` and `I` are continuous side channels or discrete tokens.
4. Keep a small-token smoke config for local testing.
