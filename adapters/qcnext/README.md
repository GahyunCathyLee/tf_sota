# QCNeXt Adapter

Goal: implement QCNeXt-style joint multi-agent prediction after QCNet works.

First implementation tasks:

1. Reuse the QCNet upstream checkout.
2. Validate QCNet adapter first.
3. Add joint multi-agent decoding and evaluation only after the shared data
   conversion is stable.
